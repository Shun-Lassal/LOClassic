import { defineConfig } from "windicss/helpers";

export default defineConfig({
  theme: {
    extend: {
      fontFamily: {
        okami: ["Okami", "sans-serif"],
      },
      colors: {
        lored: "#ff3d09",
      },
    },
  },
});
